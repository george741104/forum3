<h2>New Post</h2>

<%=form_for @post, :url => posts_path(@post), :class=>"form" do |p|%>
  <%= render :partial =>"form", :locals =>{:p=>p}%>
  <%=p.submit "Publish", :class => 'btn btn-primary'%>
  <%=link_to "Cancel", posts_path, :class => 'btn btn-default'%>
<%end%>
